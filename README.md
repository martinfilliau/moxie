moxie
=====

The new Mobile Oxford

Requirements
------------

* Solr 4 BETA
* Redis
* pip (`easy_install pip`)
* bundler (`gem install bundler`)

How to run
----------

Installation

* `pip install -r requirements.txt`
* `bundle install`

Deployment

* `make static`

